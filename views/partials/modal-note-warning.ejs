        <!-- Modal -->
        <div class="modal fade" id="noteWarningModal<%= note._id %>" data-bs-backdrop="static" data-bs-keyboard="false" tabindex="-1" aria-labelledby="note-warning-modal" aria-hidden="true">
            <div class="modal-dialog modal-dialog-scrollable modal-dialog-centered">
                <div class="modal-content">
                  <div class="modal-header">
                    <h1 id="note-warning-modal" class="modal-title fs-5">Delete note</h1>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close warning"></button>
                  </div>
                  <div class="modal-body">
                    <div class="card-text mb-3">
                      Are you sure you want to delete this note?
                    </div>
                  </div>
                  <div class="modal-footer">
                    <form action="/notes/delete/<%= note._id %>?_method=DELETE" method="POST" style="display: inline" novalidate>
                      <button type="button" class="btn btn-outline-primary" data-bs-dismiss="modal">
                        Cancel
                      </button>
                      <button type="submit" class="btn btn-danger with-spinner">
                          Delete My Note
                      </button>
                    </form>
                  </div>
                </div>
            </div>
        </div>